# Node.js Socket Proxy

Simple and easy to use TCP proxy service written in Typescript using node.js Net library

## How to use

Configure desired proxies and server port in config.json

```
npm install
```
(npm install @types/node --save-)
### Dev:

```
npm run dev
```

### Prod:

```
npm run start
```
